{
  "name": "@ymedaghri/genesis-popover",
  "private": true,
  "description": "Another popover using popper.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ymedaghri/genesis-popover.git"
  },
  "license": "BSD-3-Clause",
  "devDependencies": {
    "@polymer/gen-typescript-declarations": "^1.2.2",
    "bower": "^1.8.0",
    "webmat": "^0.2.0"
  },
  "scripts": {
    "update-types": "bower install && gen-typescript-declarations --deleteExisting --outDir .",
    "format": "webmat && npm run update-types"
  }
}
